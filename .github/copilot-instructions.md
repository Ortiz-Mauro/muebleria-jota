- Regla de prueba: Siempre que te pida generar código HTML, debes incluir un comentario en la primera línea que diga: ```
Guarda el archivo (`Ctrl + S`).

**Paso 2: Dispara el Prompt**
Abre el panel del Agente (Copilot Edits) o el Chat lateral y dile:
*"Genera la estructura básica de un footer para el proyecto."*

**Paso 3: Evalúa el Output**
Si el agente te devuelve el código y la primera línea es exactamente el comentario ``, tienes la confirmación técnica absoluta de que el archivo `.github/copilot-instructions.md` ha sido indexado y está dictando el comportamiento de la IA.

*(Una vez que lo compruebes, puedes borrar esa regla anzuelo y dejar solo tus reglas arquitectónicas reales).*

---

## Cierre Socrático

Ya que has creado exitosamente la carpeta `.github` (nota que comienza con un punto) para alojar tus instrucciones del agente:

> **Desde el punto de vista de los sistemas operativos (Linux/macOS) y la convención general de Git, ¿qué significa que una carpeta o archivo comience con un punto (`.`), como `.github` o `.gitignore`, y por qué crees que los arquitectos de software decidieron que las configuraciones tengan este formato?**